/**
 * apps/web — TanStack Start application
 *
 * Entry point skeleton. Full implementation in Phase 3.
 *
 * IMPORTANT (Render deployment):
 *   The HTTP server MUST bind to 0.0.0.0:$PORT (not localhost).
 *   See https://render.com/docs/web-services#port-binding
 */
export {};
